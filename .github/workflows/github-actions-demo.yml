name: Handle Delete Event

on:
  delete:
    branches:
      - '*'  # Отслеживать удаление всех веток
    tags:
      - '*'  # Отслеживать удаление всех тегов

jobs:
  handle-delete:
    runs-on: ubuntu-latest

    steps:
      - name: Notify deletion
        run: echo "Deleted reference"
